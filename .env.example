# Copy to .env and fill in. .env is gitignored.
#
# Path to a Firebase service account key.
# Firebase console -> Project settings -> Service accounts -> Generate new private key.
# Keep the key file OUTSIDE this repository. This variable stores only its path.
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\serviceAccountKey.json
FIREBASE_PROJECT_ID=diet-app-a908a
